
import React from 'react';
import ShiftSwapCalendar from './ShiftSwapCalendar';

// This is a simple wrapper component to maintain backwards compatibility
const SwapCalendar = () => {
  return <ShiftSwapCalendar />;
};

export default SwapCalendar;
